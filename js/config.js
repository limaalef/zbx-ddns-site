// ============================================================
//  CONFIG — DDNS Subdomain Manager
//  Ajuste estes valores para o seu ambiente antes do deploy.
// ============================================================
const CONFIG = {
    // URL base do Worker (API)
    API_BASE: 'https://api.zbx-ddns.com',

    // Client ID do Google OAuth (mesmo client usado no Worker)
    GOOGLE_CLIENT_ID: '257977612940-250678h1mup333i4267k3pdalbd3fava.apps.googleusercontent.com',

    // Domínio fixo onde os subdomínios são criados (apenas para exibição)
    BASE_DOMAIN: 'zbx-ddns.com',
};
